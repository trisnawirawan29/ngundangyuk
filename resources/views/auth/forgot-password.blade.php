<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Lupa Password · NexaAdmin</title><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"><link rel="stylesheet" href="{{ asset('css/admin.css') }}"></head><body class="login-page"><div class="login-panel mx-auto"><div class="brand-lockup"><span class="brand-mark">✦</span><span>Nexa<span>Admin</span></span></div><div class="login-heading"><p class="eyebrow">PEMULIHAN AKUN</p><h1>Lupa password?</h1><p>Masukkan email Anda dan kami akan mengirimkan link untuk membuat password baru.</p></div>@if(session('status'))<div class="alert alert-success small">{{ session('status') }}</div>@endif@if($errors->any())<div class="alert alert-danger small">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('password.email') }}">@csrf<label class="form-label" for="email">Alamat email</label><input id="email" type="email" name="email" class="form-control mb-4" value="{{ old('email') }}" required autofocus placeholder="nama@perusahaan.com"><button class="btn btn-primary w-100 py-2 fw-semibold">Kirim link reset</button></form><a href="{{ route('login') }}" class="d-block text-center small mt-4 text-decoration-none">← Kembali ke login</a></div></body></html>
