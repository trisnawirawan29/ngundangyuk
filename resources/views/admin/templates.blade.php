@extends('layouts.admin')
@section('title', 'Template Undangan')
@section('page-title', 'Template Undangan')
@section('page-subtitle', 'Kelola desain yang dapat dipilih oleh user.')
@section('content')
<div class="d-flex justify-content-end mb-4"><a href="{{ route('admin.templates.create') }}" class="btn btn-primary"><i class="fas fa-plus me-2"></i>Tambah Template</a></div>
<div class="row g-4">@forelse($templates as $template)<div class="col-12 col-md-6 col-xl-4"><div class="content-card h-100"><div class="invitation-card-preview" style="--invitation-accent:{{ $template->accent_color }};background:linear-gradient(135deg, {{ $template->accent_color }}, #241f2b)"><span>{{ $template->slug }}</span><strong>{{ $template->name }}</strong></div><div class="p-3"><p class="text-muted small mb-3">{{ $template->description ?: 'Belum ada deskripsi.' }}</p><div class="d-flex justify-content-between align-items-center mb-3"><span class="badge {{ $template->is_active ? 'text-bg-success' : 'text-bg-secondary' }}">{{ $template->is_active ? 'Aktif untuk user' : 'Nonaktif' }}</span><span class="small text-muted"><i class="fas fa-palette me-1"></i>{{ $template->accent_color }}</span></div><div class="d-flex gap-2"><a href="{{ route('admin.templates.preview', $template) }}" target="_blank" class="btn btn-sm btn-outline-secondary flex-fill"><i class="fas fa-eye me-1"></i>Preview</a><a href="{{ route('admin.templates.edit', $template) }}" class="btn btn-sm btn-primary"><i class="fas fa-pen"></i></a><form method="POST" action="{{ route('admin.templates.destroy', $template) }}" data-confirm="Hapus template ini?"><button class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button>@csrf @method('DELETE')</form></div></div></div></div>@empty<div class="col-12"><div class="content-card text-center py-5">Belum ada template.</div></div>@endforelse</div>
@endsection
